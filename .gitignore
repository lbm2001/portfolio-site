# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# env
.env*.local

# OpenNext
.open-next

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example

# Resume: public/resume.pdf is committed (regenerated locally via scripts/build-cv.sh)
# because the Cloudflare build environment has no latexmk. Only the LaTeX
# byproducts below are ignored.
/public/resume.aux
/public/resume.fdb_latexmk
/public/resume.fls
/public/resume.log
/public/resume.out
/public/resume.synctex.gz

# Drop-in portfolio/ folders staged for the project repos (copy out, then delete)
portfolio-staging/
